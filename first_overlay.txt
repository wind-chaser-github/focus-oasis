"import { useEffect, useRef } from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\nimport { Play, Pause, Square, RotateCcw } from 'lucide-react';\nimport gsap from 'gsap';\n\nexport const Overlay = () => {\n  const { status, startFocus, pauseFocus, resumeFocus, stopFocus } = useTimerStore();\n  const overlayRef = useRef<HTMLDivElement>(null);\n  \n  // Example durations\n  const presetDurations = [1, 25, 45, 60]; // 1 minute is for testing\n\n  useEffect(() => {\n    if (status === 'focusing') {\n      gsap.to(overlayRef.current, {\n        opacity: 0,\n        scale: 0.95,\n        pointerEvents: 'none',\n        duration: 1.5,\n        ease: 'power3.inOut'\n      });\n    } else {\n      gsap.to(overlayRef.current, {\n        opacity: 1,\n        scale: 1,\n        pointerEvents: 'auto',\n        duration: 1.2,\n        ease: 'power3.out'\n      });\n    }\n  }, [status]);\n\n  return (\n    <div \n      ref={overlayRef}\n      className=\"absolute inset-0 flex flex-col items-center justify-center z-10 bg-surface/30 backdrop-blur-sm pointer-events-auto\"\n    >\n      {status === 'idle' && (\n        <div className=\"flex flex-col items-center space-y-8 bg-surface/80 p-10 rounded-3xl shadow-2xl border border-white/10\">\n          <h1 className=\"text-4xl font-light tracking-widest text-white/90\">FOCUS OASIS</h1>\n          <p className=\"text-white/60 tracking-wider\">Select duration to plant your focus</p>\n          <div className=\"flex gap-4\">\n            {presetDurations.map((mins) => (\n              <button\n                key={mins}\n                onClick={() => startFocus(mins)}\n                className=\"w-16 h-16 rounded-full bg-primary/20 hover:bg-primary/40 text-primary-dark hover:text-white border border-primary/30 transition-all duration-300 flex items-center justify-center text-lg font-medium\"\n              >\n                {mins}\n              </button>\n            ))}\n          </div>\n        </div>\n      )
"import { useEffect, useRef, useState } from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\nimport { Play, Square } from 'lucide-react';\nimport gsap from 'gsap';\n\nexport const Overlay = () => {\n  const { status, currentTask, setTask, startFocus, resumeFocus, stopFocus } = useTimerStore();\n  const overlayRef = useRef<HTMLDivElement>(null);\n  \n  // Example durations\n  const presetDurations = [1, 25, 45, 60];\n\n  const [localTask, setLocalTask] = useState(currentTask);\n\n  useEffect(() => {\n    if (status === 'focusing') {\n      gsap.to(overlayRef.current, {\n        opacity: 0,\n        scale: 0.95,\n        pointerEvents: 'none',\n        duration: 1.5,\n        ease: 'power3.inOut'\n      });\n    } else {\n      gsap.to(overlayRef.current, {\n        opacity: 1,\n        scale: 1,\n        pointerEvents: 'auto',\n        duration: 1.2,\n        ease: 'power3.out'\n      });\n    }\n  }, [status]);\n\n  const handleStart = (mins: number) => {\n    setTask(localTask || 'Deep Work');\n    startFocus(mins);\n  };\n\n  return (\n    <div \n      ref={overlayRef}\n      className=\"absolute inset-0 flex flex-col items-center justify-center z-10 pointer-events-auto\"\n    >\n      {status === 'idle' && (\n        <div className=\"flex flex-col items-center space-y-10 glass p-12 rounded-[2.5rem]\">\n          <div className=\"text-center space-y-2\">\n            <h1 className=\"text-5xl font-light tracking-[0.2em] text-zinc-100 mb-2\">FOCUS OASIS</h1>\n            <p className=\"text-zinc-400 tracking-widest text-sm uppercase\">Plant your intentions</p>\n          </div>\n\n          <div className=\"w-full max-w-xs\">\n            <input \n              type=\"text\" \n              value={localTask}\n              onChange={(e) => setLocalTask(e.target.value)}\n              placeholder=\"What are you focusing on?\"\n              className=\"w-full bg-black/20 border border-white/10 rounded-2xl px-6 py-4 text-center text-zinc-200 plac
"import { useEffect, useRef, useState } from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\nimport { Play, Square } from 'lucide-react';\nimport gsap from 'gsap';\n\nexport const Overlay = () => {\n  const { status, currentTask, setTask, startFocus, resumeFocus, stopFocus } = useTimerStore();\n  const overlayRef = useRef<HTMLDivElement>(null);\n  \n  // Example durations\n  const presetDurations = [1, 25, 45, 60];\n\n  const [localTask, setLocalTask] = useState(currentTask);\n\n  useEffect(() => {\n    if (status === 'focusing') {\n      gsap.to(overlayRef.current, {\n        opacity: 0,\n        scale: 0.95,\n        pointerEvents: 'none',\n        duration: 1.5,\n        ease: 'power3.inOut'\n      });\n    } else {\n      gsap.to(overlayRef.current, {\n        opacity: 1,\n        scale: 1,\n        pointerEvents: 'auto',\n        duration: 1.2,\n        ease: 'power3.out'\n      });\n    }\n  }, [status]);\n\n  const handleStart = (mins: number) => {\n    setTask(localTask || 'Deep Work');\n    startFocus(mins);\n  };\n\n  return (\n    <div \n      ref={overlayRef}\n      className=\"absolute inset-0 flex flex-col items-center justify-center z-10 pointer-events-auto\"\n    >\n      {status === 'idle' && (\n        <div className=\"flex flex-col items-center space-y-10 glass p-12 rounded-[2.5rem]\">\n          <div className=\"text-center space-y-2\">\n            <h1 className=\"text-5xl font-light tracking-[0.2em] text-zinc-100 mb-2\">FOCUS OASIS</h1>\n            <p className=\"text-zinc-400 tracking-widest text-sm uppercase\">Plant your intentions</p>\n          </div>\n\n          <div className=\"w-full max-w-xs\">\n            <input \n              type=\"text\" \n              value={localTask}\n              onChange={(e) => setLocalTask(e.target.value)}\n              placeholder=\"What are you focusing on?\"\n              className=\"w-full bg-black/20 border border-white/10 rounded-2xl px-6 py-4 text-center text-zinc-200 plac
"import { useEffect, useRef, useState } from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\nimport { Play, Square, RotateCcw, CheckCircle2, Circle, Plus, Trash2 } from 'lucide-react';\nimport gsap from 'gsap';\nimport useSound from 'use-sound';\n\nexport const Overlay = () => {\n  const { \n    status, isDead, tasks, activeTaskId, addTask, toggleTask, deleteTask, setActiveTask, \n    startFocus, resumeFocus, stopFocus, resetToIdle \n  } = useTimerStore();\n  \n  const overlayRef = useRef<HTMLDivElement>(null);\n  const [newTaskTitle, setNewTaskTitle] = useState('');\n  \n  // Audio placeholders - usually you'd load actual mp3s. We'll disable throwing errors if missing.\n  // const [playClick] = useSound('/click.mp3', { volume: 0.25 });\n  \n  const presetDurations = [1, 25, 45, 60];\n\n  useEffect(() => {\n    if (status === 'focusing') {\n      gsap.to(overlayRef.current, {\n        opacity: 0,\n        scale: 0.95,\n        pointerEvents: 'none',\n        duration: 1.5,\n        ease: 'power3.inOut'\n      });\n    } else {\n      gsap.to(overlayRef.current, {\n        opacity: 1,\n        scale: 1,\n        pointerEvents: 'auto',\n        duration: 1.2,\n        ease: 'power3.out'\n      });\n    }\n  }, [status]);\n\n  const handleStart = (mins: number) => {\n    if (tasks.length > 0 && !activeTaskId) {\n      setActiveTask(tasks[0].id);\n    }\n    startFocus(mins);\n  };\n\n  const handleAddTask = (e: React.FormEvent) => {\n    e.preventDefault();\n    if (newTaskTitle.trim()) {\n      addTask(newTaskTitle);\n      setNewTaskTitle('');\n    }\n  };\n\n  return (\n    <div \n      ref={overlayRef}\n      className=\"absolute inset-0 flex flex-col items-center justify-center z-10 pointer-events-auto overflow-hidden\"\n    >\n      {status === 'idle' && (\n        <div className=\"flex flex-col items-center space-y-8 glass p-10 md:p-12 rounded-[2.5rem] w-[90%] max-w-md max-h-[90vh] overflow-y-auto custom-scrollbar\">\n          <div classNa
"import { useRef, useState } from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\nimport { Play, Square, RotateCcw, Check, Plus, Trash2 } from 'lucide-react';\nimport gsap from 'gsap';\nimport { useGSAP } from '@gsap/react';\n\ngsap.registerPlugin(useGSAP);\n\nexport const Overlay = () => {\n  const { \n    status, isDead, tasks, activeTaskId, addTask, toggleTask, deleteTask, setActiveTask, \n    startFocus, resumeFocus, stopFocus, resetToIdle \n  } = useTimerStore();\n  \n  const containerRef = useRef<HTMLDivElement>(null);\n  const leftPanelRef = useRef<HTMLDivElement>(null);\n  const centerStatusRef = useRef<HTMLDivElement>(null);\n  const [newTaskTitle, setNewTaskTitle] = useState('');\n  \n  const presetDurations = [1, 25, 45, 60];\n\n  useGSAP(() => {\n    if (!containerRef.current) return;\n    \n    if (status === 'focusing') {\n      // Fade out and slide down\n      gsap.to([leftPanelRef.current, centerStatusRef.current], {\n        opacity: 0,\n        y: 30,\n        pointerEvents: 'none',\n        duration: 1.5,\n        ease: 'power3.inOut',\n        stagger: 0.1\n      });\n    } else {\n      // Fade in and slide up\n      gsap.to([leftPanelRef.current, centerStatusRef.current], {\n        opacity: 1,\n        y: 0,\n        pointerEvents: 'auto',\n        duration: 1.5,\n        ease: 'power3.out',\n        stagger: 0.1\n      });\n    }\n  }, { dependencies: [status], scope: containerRef });\n\n  const handleStart = (mins: number) => {\n    if (tasks.length > 0 && !activeTaskId) {\n      setActiveTask(tasks[0].id);\n    }\n    startFocus(mins);\n  };\n\n  const handleAddTask = (e: React.FormEvent) => {\n    e.preventDefault();\n    if (newTaskTitle.trim()) {\n      addTask(newTaskTitle);\n      setNewTaskTitle('');\n    }\n  };\n\n  return (\n    <div \n      ref={containerRef}\n      className=\"absolute inset-0 pointer-events-none overflow-hidden\"\n    >\n      {/* LEFT PANEL: Task Queue (Asymmetrical Layout) */}\n      <di
"import { useRef, useState } from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\nimport { Plus, Trash2 } from 'lucide-react';\nimport gsap from 'gsap';\nimport { useGSAP } from '@gsap/react';\nimport { AnimatedText } from './AnimatedText';\n\ngsap.registerPlugin(useGSAP);\n\nexport const Overlay = () => {\n  const { \n    status, isDead, tasks, activeTaskId, addTask, toggleTask, deleteTask, setActiveTask, \n    startFocus\n  } = useTimerStore();\n  \n  const containerRef = useRef<HTMLDivElement>(null);\n  const leftPanelRef = useRef<HTMLDivElement>(null);\n  const centerStatusRef = useRef<HTMLDivElement>(null);\n  const [newTaskTitle, setNewTaskTitle] = useState('');\n  \n  const presetDurations = [1, 25, 45, 60];\n\n  useGSAP(() => {\n    if (!containerRef.current) return;\n    \n    if (status === 'focusing') {\n      gsap.to([leftPanelRef.current, centerStatusRef.current], {\n        opacity: 0,\n        x: -50,\n        pointerEvents: 'none',\n        duration: 1.5,\n        ease: 'power4.inOut',\n        stagger: 0.1\n      });\n    } else {\n      gsap.to([leftPanelRef.current, centerStatusRef.current], {\n        opacity: 1,\n        x: 0,\n        pointerEvents: 'auto',\n        duration: 1.5,\n        ease: 'power4.out',\n        stagger: 0.1\n      });\n    }\n  }, { dependencies: [status], scope: containerRef });\n\n  const handleStart = (mins: number) => {\n    if (tasks.length > 0 && !activeTaskId) {\n      setActiveTask(tasks[0].id);\n    }\n    startFocus(mins);\n  };\n\n  const handleAddTask = (e: React.FormEvent) => {\n    e.preventDefault();\n    if (newTaskTitle.trim()) {\n      addTask(newTaskTitle);\n      setNewTaskTitle('');\n    }\n  };\n\n  return (\n    <div \n      ref={containerRef}\n      className=\"absolute inset-0 pointer-events-none overflow-hidden\"\n    >\n      {/* LEFT PANEL: Brutalist Grid Task Queue */}\n      <div \n        ref={leftPanelRef}\n        className=\"absolute top-0 left-0 w-96 h-full flex fl
"import React from 'react';\n\nexport const Overlay = () => {\n  return (\n    <div className=\"absolute inset-0 pointer-events-none overflow-hidden flex items-center justify-center\">\n      \n      {/* Central Scattered Letters (Simulating the Hashgraph loading screen) */}\n      <div className=\"relative w-[300px] h-[100px] flex items-center justify-center\">\n        {/* We use exact positioning to mimic the scattered cinematic look */}\n        <span className=\"absolute top-0 left-4 text-[#eee] font-light text-2xl tracking-widest opacity-80\">O</span>\n        <span className=\"absolute top-8 left-[60px] text-[#eee] font-light text-2xl tracking-widest opacity-90\">A</span>\n        <span className=\"absolute -top-2 left-[120px] text-[#eee] font-light text-3xl tracking-widest opacity-100\">S</span>\n        <span className=\"absolute top-12 left-[180px] text-[#eee] font-light text-xl tracking-widest opacity-70\">I</span>\n        <span className=\"absolute top-2 left-[240px] text-[#eee] font-light text-2xl tracking-widest opacity-90\">S</span>\n      </div>\n\n      {/* Center \"LOADING\" Text */}\n      <div className=\"absolute top-1/2 left-1/2 -translate-x-1/2 translate-y-[80px]\">\n        <p className=\"text-[#9bb8e1] font-light tracking-[0.4em] text-[11px] opacity-60 uppercase\">\n          LOADING\n        </p>\n      </div>\n\n      {/* Bottom Footer Text */}\n      <div className=\"absolute bottom-12 left-1/2 -translate-x-1/2 w-full text-center\">\n        <p className=\"text-[#9bb8e1] font-light tracking-[0.3em] text-[9px] opacity-40 uppercase\">\n          THIS EXPERIENCE INCLUDES SOUND...\n        </p>\n      </div>\n\n    </div>\n  );\n};"
"import React from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\n\nexport const Overlay = () => {\n  const { status, startFocus } = useTimerStore();\n\n  const handleStart = (mins: number) => {\n    startFocus(mins * 60);\n  };\n\n  return (\n    <div className=\"absolute inset-0 pointer-events-none overflow-hidden flex flex-col items-center justify-center\">\n      \n      {/* Central Scattered Letters (Simulating the Hashgraph loading screen) */}\n      <div \n        className=\"relative w-[300px] h-[100px] flex items-center justify-center transition-opacity duration-1000\"\n        style={{ opacity: status === 'idle' ? 1 : 0 }}\n      >\n        {/* We use exact positioning to mimic the scattered cinematic look */}\n        <span className=\"absolute top-0 left-4 text-[#eee] font-light text-2xl tracking-widest opacity-80\">O</span>\n        <span className=\"absolute top-8 left-[60px] text-[#eee] font-light text-2xl tracking-widest opacity-90\">A</span>\n        <span className=\"absolute -top-2 left-[120px] text-[#eee] font-light text-3xl tracking-widest opacity-100\">S</span>\n        <span className=\"absolute top-12 left-[180px] text-[#eee] font-light text-xl tracking-widest opacity-70\">I</span>\n        <span className=\"absolute top-2 left-[240px] text-[#eee] font-light text-2xl tracking-widest opacity-90\">S</span>\n      </div>\n\n      {/* Duration Selection replacing \"LOADING\" */}\n      <div \n        className=\"absolute top-1/2 left-1/2 -translate-x-1/2 translate-y-[80px] pointer-events-auto flex gap-8 transition-opacity duration-1000\"\n        style={{ opacity: status === 'idle' ? 1 : 0 }}\n      >\n        {[25, 45, 60].map((mins) => (\n          <button\n            key={mins}\n            onClick={() => handleStart(mins)}\n            className=\"text-[#9bb8e1] font-light tracking-[0.4em] text-[11px] opacity-60 hover:opacity-100 hover:text-[#eee] transition-all uppercase\"\n          >\n            {mins}M\n          
"import React from 'react';\n\nexport const Overlay = () => {\n  return (\n    <div className=\"absolute inset-0 pointer-events-none overflow-hidden flex items-center justify-center\">\n      \n      {/* Central Scattered Letters (Simulating the Hashgraph loading screen) */}\n      <div className=\"relative w-[300px] h-[100px] flex items-center justify-center\">\n        {/* We use exact positioning to mimic the scattered cinematic look */}\n        <span className=\"absolute top-0 left-4 text-[#eee] font-light text-2xl tracking-widest opacity-80\">O</span>\n        <span className=\"absolute top-8 left-[60px] text-[#eee] font-light text-2xl tracking-widest opacity-90\">A</span>\n        <span className=\"absolute -top-2 left-[120px] text-[#eee] font-light text-3xl tracking-widest opacity-100\">S</span>\n        <span className=\"absolute top-12 left-[180px] text-[#eee] font-light text-xl tracking-widest opacity-70\">I</span>\n        <span className=\"absolute top-2 left-[240px] text-[#eee] font-light text-2xl tracking-widest opacity-90\">S</span>\n      </div>\n\n      {/* Center \"LOADING\" Text */}\n      <div className=\"absolute top-1/2 left-1/2 -translate-x-1/2 translate-y-[80px]\">\n        <p className=\"text-[#9bb8e1] font-light tracking-[0.4em] text-[11px] opacity-60 uppercase\">\n          LOADING\n        </p>\n      </div>\n\n      {/* Bottom Footer Text */}\n      <div className=\"absolute bottom-12 left-1/2 -translate-x-1/2 w-full text-center\">\n        <p className=\"text-[#9bb8e1] font-light tracking-[0.3em] text-[9px] opacity-40 uppercase\">\n          THIS EXPERIENCE INCLUDES SOUND...\n        </p>\n      </div>\n\n    </div>\n  );\n};"
"import React from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\n\nexport const Overlay = () => {\n  const { status, startFocus } = useTimerStore();\n\n  const handleStart = (mins: number) => {\n    startFocus(mins * 60);\n  };\n\n  return (\n    <div className=\"absolute inset-0 pointer-events-none overflow-hidden flex flex-col items-center justify-center\">\n      \n      {/* Central Scattered Letters (Simulating the Hashgraph loading screen) */}\n      <div \n        className=\"relative w-[300px] h-[100px] flex items-center justify-center transition-opacity duration-1000\"\n        style={{ opacity: status === 'idle' ? 1 : 0 }}\n      >\n        {/* We use exact positioning to mimic the scattered cinematic look */}\n        <span className=\"absolute top-0 left-4 text-[#eee] font-light text-2xl tracking-widest opacity-80\">O</span>\n        <span className=\"absolute top-8 left-[60px] text-[#eee] font-light text-2xl tracking-widest opacity-90\">A</span>\n        <span className=\"absolute -top-2 left-[120px] text-[#eee] font-light text-3xl tracking-widest opacity-100\">S</span>\n        <span className=\"absolute top-12 left-[180px] text-[#eee] font-light text-xl tracking-widest opacity-70\">I</span>\n        <span className=\"absolute top-2 left-[240px] text-[#eee] font-light text-2xl tracking-widest opacity-90\">S</span>\n      </div>\n\n      {/* Duration Selection replacing \"LOADING\" */}\n      <div \n        className=\"absolute top-1/2 left-1/2 -translate-x-1/2 translate-y-[80px] pointer-events-auto flex gap-8 transition-opacity duration-1000\"\n        style={{ opacity: status === 'idle' ? 1 : 0 }}\n      >\n        {[25, 45, 60].map((mins) => (\n          <button\n            key={mins}\n            onClick={() => handleStart(mins)}\n            className=\"text-[#9bb8e1] font-light tracking-[0.4em] text-[11px] opacity-60 hover:opacity-100 hover:text-[#eee] transition-all uppercase\"\n          >\n            {mins}M\n          
"import React from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\n\nexport const Overlay = () => {\n  const { status, startFocus } = useTimerStore();\n\n  const handleStart = (mins: number) => {\n    startFocus(mins * 60);\n  };\n\n  return (\n    <div className=\"absolute inset-0 pointer-events-none overflow-hidden flex flex-col items-center justify-center\">\n      \n      {/* Central Scattered Letters (Simulating the Hashgraph loading screen exactly) */}\n      <div \n        className=\"relative w-[300px] h-[150px] flex items-center justify-center transition-opacity duration-1000 font-sans font-bold\"\n        style={{ opacity: status === 'idle' ? 1 : 0 }}\n      >\n        <span className=\"absolute top-[20px] left-[50px] text-[#555] text-4xl\">F</span>\n        <span className=\"absolute top-[20px] left-[90px] text-[#fff] text-4xl\">O</span>\n        <span className=\"absolute top-[20px] left-[130px] text-[#fff] text-4xl\">C</span>\n        <span className=\"absolute top-[20px] left-[170px] text-[#777] text-4xl\">U</span>\n        <span className=\"absolute top-[20px] left-[210px] text-[#fff] text-4xl\">S</span>\n        \n        <span className=\"absolute top-[70px] left-[40px] text-[#999] text-4xl\">P</span>\n        <span className=\"absolute top-[70px] right-[40px] text-[#fff] text-4xl\">R</span>\n      </div>\n\n      {/* Center \"LOADING\" Text style, replaced with duration buttons */}\n      <div \n        className=\"absolute top-1/2 left-1/2 -translate-x-1/2 translate-y-[80px] pointer-events-auto flex flex-col items-center transition-opacity duration-1000\"\n        style={{ opacity: status === 'idle' ? 1 : 0 }}\n      >\n        <div className=\"flex gap-8 mb-4\">\n          {[25, 45, 60].map((mins) => (\n            <button\n              key={mins}\n              onClick={() => handleStart(mins)}\n              className=\"text-[#eee] font-bold tracking-[0.2em] text-[10px] opacity-60 hover:opacity-100 transition-all uppercas
"import React from 'react';\n\nexport const Overlay = () => {\n  return (\n    <div className=\"absolute inset-0 pointer-events-none overflow-hidden flex flex-col items-center justify-center bg-[#05080f]\">\n      \n      {/* Central Scattered Letters (Simulating the Hashgraph loading screen exactly) */}\n      <div className=\"relative w-[300px] h-[150px] flex items-center justify-center font-sans font-bold select-none\">\n        {/* S H G A P */}\n        <span className=\"absolute top-[20px] left-[40px] text-[#555] text-4xl\">S</span>\n        \n        {/* The 'H' in hashgraph has a split/glitchy look in the screenshot, we simulate with a tight span group */}\n        <span className=\"absolute top-[20px] left-[85px] flex\">\n          <span className=\"text-[#333] text-4xl transform -translate-x-[2px]\">H</span>\n          <span className=\"text-[#555] text-4xl absolute left-[2px] opacity-50\">H</span>\n        </span>\n        \n        <span className=\"absolute top-[20px] left-[130px] text-[#fff] text-4xl\">G</span>\n        <span className=\"absolute top-[20px] left-[175px] text-[#666] text-4xl\">A</span>\n        <span className=\"absolute top-[20px] left-[215px] text-[#fff] text-4xl\">P</span>\n        \n        {/* V R */}\n        <span className=\"absolute top-[80px] left-[10px] text-[#999] text-4xl\">V</span>\n        <span className=\"absolute top-[80px] left-[195px] text-[#fff] text-4xl\">R</span>\n      </div>\n\n      {/* Center \"LOADING\" Text style */}\n      <div className=\"absolute top-1/2 left-1/2 -translate-x-1/2 translate-y-[80px] flex flex-col items-center select-none\">\n        <p className=\"text-[#eee] font-bold text-[10px] mb-3 tracking-[0.3em]\">LOADING</p>\n        <div className=\"w-[120px] h-[1px] bg-[#333]\"></div>\n      </div>\n\n      {/* Bottom Footer Text */}\n      <div className=\"absolute bottom-12 left-1/2 -translate-x-1/2 w-full text-center select-none\">\n        <p className=\"text-[#555] font-bold tracking-[0.1em]
"import React, { useState } from 'react';\nimport { useTimerStore } from '../../store/useTimerStore';\n\nexport const Overlay = () => {\n  const { status, addTask, startFocus } = useTimerStore();\n  const [title, setTitle] = useState('');\n\n  const handleStart = (mins: number) => {\n    if (title.trim()) {\n      addTask(title.trim());\n    }\n    startFocus(mins * 60);\n  };\n\n  return (\n    <div className=\"absolute inset-0 pointer-events-none overflow-hidden flex flex-col items-center justify-center\">\n      \n      {/* Central Scattered Letters */}\n      <div \n        className=\"relative w-[300px] h-[150px] flex items-center justify-center font-sans font-bold select-none transition-opacity duration-1000\"\n        style={{ opacity: status === 'idle' ? 1 : 0 }}\n      >\n        <span className=\"absolute top-[20px] left-[40px] text-[#555] text-4xl\">S</span>\n        <span className=\"absolute top-[20px] left-[85px] flex\">\n          <span className=\"text-[#333] text-4xl transform -translate-x-[2px]\">H</span>\n          <span className=\"text-[#555] text-4xl absolute left-[2px] opacity-50\">H</span>\n        </span>\n        <span className=\"absolute top-[20px] left-[130px] text-[#fff] text-4xl\">G</span>\n        <span className=\"absolute top-[20px] left-[175px] text-[#666] text-4xl\">A</span>\n        <span className=\"absolute top-[20px] left-[215px] text-[#fff] text-4xl\">P</span>\n        <span className=\"absolute top-[80px] left-[10px] text-[#999] text-4xl\">V</span>\n        <span className=\"absolute top-[80px] left-[195px] text-[#fff] text-4xl\">R</span>\n      </div>\n\n      {/* Focus Setup Area */}\n      <div \n        className=\"absolute top-1/2 left-1/2 -translate-x-1/2 translate-y-[80px] pointer-events-auto flex flex-col items-center transition-opacity duration-1000\"\n        style={{ opacity: status === 'idle' ? 1 : 0 }}\n      >\n        <input \n          type=\"text\"\n          value={title}\n          onChange={(e) => setTitl
